# 更新日志

## v0.9.8 — 2026-09-04

- **修复德语 / 法语界面下 Thesis 与 Holders 全空**。v0.9.6 号称支持六种界面语言，德法两语其实一次也没生效过：`Ø Haltedauer`（平均持仓）、`Ø Einstieg`（平均买入价）、`Marktkap.`（市值）、`Détention moy.` 这几个词条，词首是 `Ø`/`À`、词尾是句点，而当时的词界判定只认 ASCII 字母，反倒要求相邻字符必须是字母数字，于是永远匹配不上。中文 / 日文因为不加词界侥幸没事，而回归测试恰好只测了中日文——所以一路全绿发到线上。现在词界改为"两侧不得紧邻 ASCII 字母数字"，德、法、西三语的变音与缩写词条一并修好，测试补齐德语、法语两条完整链路。
- **修复英文界面下取不到每位持有人的 Thesis 列**。找列头时那道便宜预筛读的是 `textContent`，相邻单元格会被拼成 `TraderPosition`，而预筛用的是带词界的正则——拉丁字母语言（含英文本身）在这一步就被判死，后面那道精确判定根本没机会跑；同样只有中日文躲过。预筛改为不带词界（精确判定不变）。
- **修复持有时长显示被截断**：`4 Std.` 显示成 `4 S`、`22h 4min` 显示成 `22h 4m`、`3 mois` 显示成 `3 mo`。时间单位表里单字母单位排在长单位前面，正则先到先得，把长单位吃掉了一截。改为长单位优先。
- **更新提示加固**：链接渲染前复验必须是 https（后台本就只认 x.com / twitter.com 的状态链接，这是第二道）；悬停摘要清掉删除线、行内链接等 Markdown 记号；版本号比较先去掉 `-beta` 这类后缀，预发布版不会被误判成新版。
- **其它**：亮度 / 透明度滑块拖动时改为攒 200 毫秒写一次设置，不再每动一格就落盘；左栏排除的几何兜底只在认不出左栏、且窗口够宽时才启用，窄窗口下不再误伤正文评论，这道防线的状态（认出 / 兜底 / 出错）也进了诊断报告。

## v0.9.7 — 2026-09-04

- **修复更新提示的悬停摘要抓错内容**：原先取发布说明的第一行，实际拿到的是"Fixes"这类分节标题，对判断"要不要更新"毫无帮助。现在取第一条真正的变更条目。（拿真实发布说明回放才发现——此前的测试用的是桩数据，没覆盖到真实形态。）

## v0.9.6 — 2026-09-04

- **支持 fomo 的六种界面语言**（中 / 日 / 德 / 西 / 法 / 英）。此前抓取锚点全写死英文，fomo 账号语言设成中文时 Thesis 与 Holders 全空——真实用户案例。锚点词表直接取自 fomo 自己的语言包，不是猜的；持有时长的中日时间单位（小时/分钟/時間/分）也一并认了。
- **修复「最新 ≥2赞」排序抓不到发言时间**：fomo 左栏是**全局活动流**（所有币混着滚），它的条目和底部 Thesis 评论流长得一模一样，此前会把**别的币**的评论当成本币的，作者对不上号所以永远为空。现在按区域排除左栏。
- **副池交易对 chips 从顶部移到「起源」标题行右侧**，头部不再拥挤。
- **新增卡片亮度 / 透明度调节**：卡片头部 ☀ 按钮，或扩展设置面板里的滑块，两处实时同步、设置会记住。
- **右下角圆钮可拖动**，位置会记住；拖完那一下不会误触发打开卡片。
- **新增版本检测（默认开，可关）**：每 6 小时查一次本项目的最新发布版本，有新版时在卡片页尾显示一行小字，悬停可看这版改了什么。检测只读 GitHub 公开接口，**不发送任何页面或代币信息**；这是本扩展第 5 个外部端点，已写进隐私说明与说明页的接触点表格。

## v0.9.5 — 2026-09-03

- **识别浏览器翻译**：开着 Chrome 网页翻译时，fomo 表格里的英文被改成中文，扩展认不出表，Thesis / Holders 一直空（真实用户案例）。现在检测到页面被翻译，空态直接提示"显示原文再刷新"；诊断信息新增 `translated` / `cjk` / 表区文本样本三个字段。
- **两家外部代码审查（K3 + Gickey）后的加固**：
  - 点 fomo 的 Holders 标签 / Friends only 开关把表点回来后，卡片 0.4 秒内自动补扫（此前只能再滚一下）；
  - 滚动补扫只在观察器已退场、表确实没出来时做一次单扫，不再每 1.5 秒重启整套观察器；"表在但没人写 thesis"这类稳定空态不再反复扫描；
  - Friends only 判定只认按钮自己的精确颜色 token（`--color-text-tertiary` / `--color-accent-primary`），可见控件优先，认不出宁可不判；底部标签判定按同一排分组、`hover:` 前缀的 class 不算、必须唯一；空态提示先说"切回 Holders"再说 Friends only；
  - 记住的卡片尺寸/位置读取时校验有效数值，恢复到更小屏幕时钳在窗口内，窗口缩放时同步重算；拖尺寸时拖出浏览器窗口松手也能收尾；
  - `contenteditable=""` 编辑区也算页面控件；诊断信息里的路径只报路由模板，不再带出完整合约地址；"没人写 thesis"空态也有诊断按钮。

## v0.9.4 — 2026-09-03

- **修复：Friends only 状态判错**。fomo 页面上那个开关不是复选框（是按钮 + 颜色），此前一律判"不知道"，于是只要表没抓到就统一提示"若开着 Friends only…"，把没开的人也带偏了。现在按真实页面结构判定，并且只看 Holders 表右上那一个（图表叠加层那个不算）。
- **新增：底部面板停在 Swaps / Thesis 时直说**。fomo 自己的 Thesis 标签会把 Holders 表整个换掉，卡片无表可读；空态现在提示"把 fomo 下方面板切到「Holders」"，不再让你去猜。
- **修复：表晚到就一直空**。卡片弹出 25 秒后才把 Holders 表滚出来，原来没人再去读它；现在滚动一下就补扫。
- **新增：空态「诊断」按钮**。Thesis / Holders 空着时点一下，生成一段排查信息（版本、视口、表锚点数、底部标签、Friends only 状态、上次扫描）并自动复制，发给作者即可远程排查。

## v0.9.3 — 2026-09-03

- **卡片大小可以自己拖了**：右下角多了个尺寸手柄，拖一下就能改宽高，**尺寸会记住**，下次打开还是你调好的样子。默认大小不变。
- 双击卡片头部现在把**大小和位置一起**恢复默认（原先只复位位置）。
- 最小拉到 280×220，不会被拉没；拉大时不超出窗口。

## v0.9.2 — 2026-09-03

- **双击卡片头部 = 回到默认停靠位**：卡片被拖走的位置本来会一直记住，下次打开还停在那儿（比如压在 K 线中间）。现在双击头部空白处即可归位，同时清掉记住的位置；鼠标停在头部有操作提示。
- 卡片页尾恢复成只有 `By @0xHogen`（v0.9.1 加的 GitHub 链接撤掉）。

## v0.9.1 — 2026-09-03

- **点页面控件不再关卡**：此前点 fomo 页面上的任何位置都算"点卡外关闭"，勾一下 Holders 表上方的 *Friends only* 卡片就没了。现在按钮 / 勾选框 / 筛选与排序钮 / 链接等可点区块一律视为"在操作 fomo"，卡片保持；只有点图表、空白这类非交互区才关闭。× 和 `Esc` 照旧。
- 署名进产品：设置面板署名旁加 GitHub 链接；扩展描述与作者字段带上 @0xHogen；源码文件头加版权与来源声明。（卡片页脚也短暂加过 GitHub 链接，v0.9.2 已撤回。）
- 不需要更新也能继续用；想升级：新版 zip 解压覆盖同一文件夹，回 `chrome://extensions` 点 ↻。

## v0.9.0 — 2026-09-02

- **卡片 UI 全面本地化**：`中/EN` 现在连同段标题、排序钮、持有时长、灰字提示、按钮提示一起切换，英文用户装上看到的是英文壳（此前只切 DeBot 正文语言）。
- README 改为英文默认 + 中文一键切换；说明页支持中英切换（按浏览器语言自动选，可手动切并记忆）。
- 安全加固（跨家 K3 深度审查，无 critical/high；以下为其 med/low 建议全部落地）：
  - 后台消息路由只受理本扩展自身的 content script（`sender.id` 校验），合约地址形状校验；
  - 三方响应超 2MB 一律丢弃、DexScreener pairs 遍历封顶；
  - 一轮抓取只扫一遍 Holders 表（原先 holders / thesis / 列头各扫一遍），观察者去抖 250→600ms，两页就绪后提前撤观察者；
  - 翻译缓存跨代币封顶；
  - 公开版连遗留的分析源设置键也不再读取。

## v0.8.9 — 2026-09-02

- 标签页改名 **Meta**（默认）／**Thesis**／**Holders**，与 fomo 自家 tab 一个用法。
- Thesis 页新增排序切换：**按赞**（默认，读 Holders 表全量）／**最新 ≥2赞**（直接读 fomo 自家评论流，按发言时间新→旧）；能对上号的条目行头带发言时间。
- Holders 页每行改为 `@名字 · $仓位 ▲盈亏% · 持有时长`，成本移到整行悬停提示。
- 头部新增**副池交易对 chips**（DexScreener 公开接口）：只显示 $TSLA/$HIMS 这类非常规配对，ETH/稳定币主池不占位；叙事类型 chip 移除腾位置，类型悬停币名可见。
- Meta 页顺序：起源 → 评级理由 → 来源推文 → 传播 → 开发者；卡片页尾加一行 `By @0xHogen`。
- 抓取修复：fomo 的 flex 单元格在 `innerText` 下会黏成一坨（点赞黏进正文、金额出鬼数字、名字连体、列头定位失效），改为按文本节点拼接取词，Holders 表被隐藏时同样可解析；成本列 "入场市值 MC 入场价" 取价格；表暂时卸载时保留已有数据不清屏。
- Friends only：侦测到页面开着该筛选且抓不到数据时，空态直接写明"关闭它才能显示全部数据"。
- 跨家代码审查（K3）修正：翻译登记表死节点清理、盈亏方向未知不再默认绿、中文无空格 thesis 的启发式回退、抓取指纹去重防闪烁。

## v0.8.0 — 2026-09-02

- 卡片顶部改为三标签页：**叙事**（默认）／**观点**／**持仓者**，标签带条数角标。
- 观点页独占一页：按点赞降序全量列出（上限 30 条、按正文去重），行头带该持有人的仓位与盈亏。
- 首个公开发布版本：manifest 加入固定扩展 ID（更新、换目录不丢配置），补齐 LICENSE / 隐私说明 / 打包脚本。

## v0.8 之前（内部迭代摘要）

- **v0.7.x**：自动弹出改三档（精简展开／全部展开／关闭）；悬停预览 600ms 停留计时修复
  （原实现行内微动会无限重置，预览永不弹出）；翻译入口收敛为头部单一「中/EN」开关；头部按钮重排。
- **v0.7.4**：观点与持仓者两页换源至 Holders 表（token 专属、稳定），不再读全局混排 Feed。
- **v0.5**：观点／持仓改为只读页面 DOM，扩展不再向 fomo 发任何请求（零网络、不碰登录态）。
