# 更新日志

## v0.9.4 — 2026-09-03

- **修复：Friends only 状态判错**。fomo 页面上那个开关不是复选框（是按钮 + 颜色），此前一律判"不知道"，于是只要表没抓到就统一提示"若开着 Friends only…"，把没开的人也带偏了。现在按真实页面结构判定，并且只看 Holders 表右上那一个（图表叠加层那个不算）。
- **新增：底部面板停在 Swaps / Thesis 时直说**。fomo 自己的 Thesis 标签会把 Holders 表整个换掉，卡片无表可读；空态现在提示"把 fomo 下方面板切到「Holders」"，不再让你去猜。
- **修复：表晚到就一直空**。卡片弹出 25 秒后才把 Holders 表滚出来，原来没人再去读它；现在滚动一下就补扫。
- **新增：空态「诊断」按钮**。Thesis / Holders 空着时点一下，生成一段排查信息（版本、视口、表锚点数、底部标签、Friends only 状态、上次扫描）并自动复制，发给作者即可远程排查。

## v0.9.3 — 2026-09-03

- **卡片大小可以自己拖了**：右下角多了个尺寸手柄，拖一下就能改宽高，**尺寸会记住**，下次打开还是你调好的样子。默认大小不变。
- 双击卡片头部现在把**大小和位置一起**恢复默认（原先只复位位置）。
- 最小拉到 280×220，不会被拉没；拉大时不超出窗口。

## v0.9.2 — 2026-09-03

- **双击卡片头部 = 回到默认停靠位**：卡片被拖走的位置本来会一直记住，下次打开还停在那儿（比如压在 K 线中间）。现在双击头部空白处即可归位，同时清掉记住的位置；鼠标停在头部有操作提示。
- 卡片页尾恢复成只有 `By @0xHogen`（v0.9.1 加的 GitHub 链接撤掉）。

## v0.9.1 — 2026-09-03

- **点页面控件不再关卡**：此前点 fomo 页面上的任何位置都算"点卡外关闭"，勾一下 Holders 表上方的 *Friends only* 卡片就没了。现在按钮 / 勾选框 / 筛选与排序钮 / 链接等可点区块一律视为"在操作 fomo"，卡片保持；只有点图表、空白这类非交互区才关闭。× 和 `Esc` 照旧。
- 署名进产品：设置面板署名旁加 GitHub 链接；扩展描述与作者字段带上 @0xHogen；源码文件头加版权与来源声明。（卡片页脚也短暂加过 GitHub 链接，v0.9.2 已撤回。）
- 不需要更新也能继续用；想升级：新版 zip 解压覆盖同一文件夹，回 `chrome://extensions` 点 ↻。

## v0.9.0 — 2026-09-02

- **卡片 UI 全面本地化**：`中/EN` 现在连同段标题、排序钮、持有时长、灰字提示、按钮提示一起切换，英文用户装上看到的是英文壳（此前只切 DeBot 正文语言）。
- README 改为英文默认 + 中文一键切换；说明页支持中英切换（按浏览器语言自动选，可手动切并记忆）。
- 安全加固（跨家 K3 深度审查，无 critical/high；以下为其 med/low 建议全部落地）：
  - 后台消息路由只受理本扩展自身的 content script（`sender.id` 校验），合约地址形状校验；
  - 三方响应超 2MB 一律丢弃、DexScreener pairs 遍历封顶；
  - 一轮抓取只扫一遍 Holders 表（原先 holders / thesis / 列头各扫一遍），观察者去抖 250→600ms，两页就绪后提前撤观察者；
  - 翻译缓存跨代币封顶；
  - 公开版连遗留的分析源设置键也不再读取。

## v0.8.9 — 2026-09-02

- 标签页改名 **Meta**（默认）／**Thesis**／**Holders**，与 fomo 自家 tab 一个用法。
- Thesis 页新增排序切换：**按赞**（默认，读 Holders 表全量）／**最新 ≥2赞**（直接读 fomo 自家评论流，按发言时间新→旧）；能对上号的条目行头带发言时间。
- Holders 页每行改为 `@名字 · $仓位 ▲盈亏% · 持有时长`，成本移到整行悬停提示。
- 头部新增**副池交易对 chips**（DexScreener 公开接口）：只显示 $TSLA/$HIMS 这类非常规配对，ETH/稳定币主池不占位；叙事类型 chip 移除腾位置，类型悬停币名可见。
- Meta 页顺序：起源 → 评级理由 → 来源推文 → 传播 → 开发者；卡片页尾加一行 `By @0xHogen`。
- 抓取修复：fomo 的 flex 单元格在 `innerText` 下会黏成一坨（点赞黏进正文、金额出鬼数字、名字连体、列头定位失效），改为按文本节点拼接取词，Holders 表被隐藏时同样可解析；成本列 "入场市值 MC 入场价" 取价格；表暂时卸载时保留已有数据不清屏。
- Friends only：侦测到页面开着该筛选且抓不到数据时，空态直接写明"关闭它才能显示全部数据"。
- 跨家代码审查（K3）修正：翻译登记表死节点清理、盈亏方向未知不再默认绿、中文无空格 thesis 的启发式回退、抓取指纹去重防闪烁。

## v0.8.0 — 2026-09-02

- 卡片顶部改为三标签页：**叙事**（默认）／**观点**／**持仓者**，标签带条数角标。
- 观点页独占一页：按点赞降序全量列出（上限 30 条、按正文去重），行头带该持有人的仓位与盈亏。
- 首个公开发布版本：manifest 加入固定扩展 ID（更新、换目录不丢配置），补齐 LICENSE / 隐私说明 / 打包脚本。

## v0.8 之前（内部迭代摘要）

- **v0.7.x**：自动弹出改三档（精简展开／全部展开／关闭）；悬停预览 600ms 停留计时修复
  （原实现行内微动会无限重置，预览永不弹出）；翻译入口收敛为头部单一「中/EN」开关；头部按钮重排。
- **v0.7.4**：观点与持仓者两页换源至 Holders 表（token 专属、稳定），不再读全局混排 Feed。
- **v0.5**：观点／持仓改为只读页面 DOM，扩展不再向 fomo 发任何请求（零网络、不碰登录态）。
