# 隐私说明 / Privacy Policy

**一句话：本扩展没有服务器，不采集、不上报任何数据；一切配置只存你的浏览器本地。**

## 数据接触面（全部四处）

| 接触点 | 用途 | 性质 |
| --- | --- | --- |
| `app.debot.ai`（备用 `debot.ai`） | 拉取代币叙事分析 | 公开接口，不登录、不带 cookie、无 key |
| fomo.family 页面 DOM（本机） | 读取观点与持仓者 | **零网络**：只读页面上已渲染的内容，不发任何请求 |
| 你自己填的分析源（可选） | 「AI 判断」段 | 不填就完全不存在；填了才会申请该域名的授权 |
| `api.fxtwitter.com` | 叙事来源推文正文 | 公开只读，不登录、不发帖 |

## 不做的事

- **不碰 fomo 登录凭证**：扩展不向 fomo.family 发任何网络请求，不读取、不存储、不外发页面里的任何令牌。
- **不采集浏览行为**，无遥测、无统计、无指纹。
- **不注入其它网站**：content script 只匹配 `https://fomo.family/*`。
- 所有设置存放在浏览器本地 `chrome.storage`，卸载扩展即清除。

## 自定义分析源的安全边界（SSRF 防线）

自定义分析源的请求由扩展后台代发、不受页面 CORS 约束，因此默认**只接受 https 公网地址**：
内网、环回、链路本地、CGNAT 段与裸主机名一律在发出前拒绝（一个字节都不会发出）。
「⚠️ 高级·仅自用」开关（默认关）只为自建内网源的用户保留，开启后仍需逐域名显式授权。

## 反馈

任何隐私相关问题请开 GitHub Issue。
